# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Dé punt op de i
email: paulien@de-punt-op-de-i.nl
description: >- # this means to ignore newlines until "baseurl:"
  Dé punt op de i is opgericht in augustus 2018 door Paulien van Yperen. 
  Nederlands als tweede taal (NT2). 
  Correctie- en redigeerwerk van website-teksten, scripties, artikelen, boeken etc.;
  Analyse van en advies voor administratieve processen in het onderwijs;
  Voorlezen, zowel voor kinderen als voor volwassenen die niet (meer) zo goed zelf kunnen 
  lezen; inspreken van gesproken boeken of andere teksten;  
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.de-punt-op-de-i.nl" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: jekyllrb
github_username:  jekyll

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - readme.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
